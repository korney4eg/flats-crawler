class ParserConfigurator
  # This class sets and gets parser configuration
  attr_accessor :rooms, :price, :step, :areas, :years, :logger

  def read_configuration
    # This method is to define custom configuration of values:
    # rooms, price, step, areas, years, logger
  end

end
